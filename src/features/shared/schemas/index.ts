/**
 * Shared Schemas Index
 * 
 * Central export point for all entity schemas and types.
 * Use this for clean imports: `import { UserSchema, User } from '@/features/shared/schemas'`
 */

export * from './entities';
